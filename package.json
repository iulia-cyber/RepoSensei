{
  "name": "repo-sensei",
  "version": "0.1.0",
  "description": "Local-first RAG + LLM code Q&A with an interactive dependency graph, search, and clickable citations. Your code stays on your machine.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/simam/RepoSensei.git"
  },
  "keywords": [
    "codebase",
    "repository",
    "q&a",
    "dependency-graph",
    "rag",
    "localhost"
  ],
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "setup": "node scripts/setup-local.js",
    "lint": "node --check server.js && node --check web/app.js && node --check scripts/setup-local.js && node --check scripts/typecheck.js && node --check scripts/run-tests.js",
    "typecheck": "node scripts/typecheck.js",
    "test": "node scripts/run-tests.js",
    "check": "npm run lint && npm run typecheck && npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
